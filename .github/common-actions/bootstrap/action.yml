name: Bootstrap
description: Setup Node.js and install dependencies via pnpm. Run AFTER actions/checkout — local composite actions cannot replace checkout.

inputs:
  node-version:
    description: Node major version to install.
    required: false
    default: "24"
  pnpm-version:
    description: pnpm version to install. Pinned so consumers without a packageManager field still resolve a deterministic release.
    required: false
    default: "10"

runs:
  using: composite
  steps:
    - name: Setup pnpm
      uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1
      with:
        version: ${{ inputs.pnpm-version }}
        run_install: false

    - name: Setup Node.js
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
      with:
        node-version: ${{ inputs.node-version }}
        cache: pnpm

    - name: Install dependencies
      shell: bash
      run: pnpm install --frozen-lockfile
